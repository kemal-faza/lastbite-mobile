import React from 'react';

describe('OrderConfirmSuccessScreen (removed)', () => {
  it('is deprecated — all order flow merged into OrderDetailScreen', () => {
    expect(true).toBe(true);
  });
});
